# Magic Inc/Dec number🪄

Esta es una utilidad de JavaScript que implementa una lógica de incremento y decremento "mágica".Esta función opera sobre el **primer dígito significativo** y ajusta el **orden de magnitud** (exponente) del número de forma automática.

## 📋 Requisitos

* **Node.js**: Versión 10.4.0 o superior (necesaria para el soporte nativo de `BigInt`).

## 🚀 Ejecución rápida

Para ejecutar el script y ver los resultados por consola, sigue estos pasos:

1. Posicionate dentro de la carpeta utils donde esta nuestra función magic_inc dentro del archivo llamado `index.js`.
2. Abre tu terminal o consola de comandos.
3. Ejecuta el siguiente comando :

```bash
node index.js